import { PopupButton } from '@typeform/embed-react'

const ListingSubmission = () => {
  return (
    <PopupButton id="UR7SpT93" style={{ fontSize: 20 }} className="my-button">
      click to open form in popup
    </PopupButton>
  )
}

export default ListingSubmission;